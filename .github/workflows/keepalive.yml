name: Keep Render App Awake

on:
  schedule:
    # Every 10 minutes
    - cron: "*/12 * * * *"
  workflow_dispatch: # allows manual run too

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl your Render app
        run: curl -s https://blog-website-back-end.onrender.com/ > /dev/null
